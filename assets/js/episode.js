const root=document.querySelector('#episodeRoot');const id=new URLSearchParams(location.search).get('id');
loadEpisodes().then(items=>{const e=items.find(x=>String(x.trackId)===String(id));if(!e){root.innerHTML='<div class="empty">Episode not found. <a href="episodes.html">Return to all episodes</a>.</div>';return}document.title=`${e.trackName} | Uncovered Legacy`;root.innerHTML=`<div class="episode-detail"><img src="${esc((e.artworkUrl600||e.artworkUrl100||'').replace('100x100','1200x1200'))}" alt="${esc(e.trackName)} official episode artwork"><div><div class="eyebrow">Season ${seasonOf(e)} · Episode ${episodeNo(e)} · ${fmtDate(e.releaseDate)}</div><h1>${esc(e.trackName)}</h1><div class="hero-actions"><a class="btn btn-gold" target="_blank" rel="noopener" href="${esc(e.trackViewUrl||APPLE_SHOW)}">Listen on Apple Podcasts</a><a class="btn" href="episodes.html?season=${seasonOf(e)}">Season ${seasonOf(e)}</a></div><div class="description">${esc(cleanText(e.description||e.shortDescription||''))}</div></div></div>`}).catch(err=>root.innerHTML=`<div class="empty">${esc(err.message)}</div>`);
